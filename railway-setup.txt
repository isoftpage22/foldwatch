Foldwatch on Railway — quick checklist
========================================

Architecture: MySQL (Railway) + Backend (Docker) + Frontend (Docker).
Repo root must stay the build context (do NOT set Root Directory to apps/*).

--- Config as code (in this repo) ---
• /railway.json — backend: builder DOCKERFILE, dockerfilePath docker/backend.Dockerfile,
  watchPatterns so only backend-related paths trigger deploys.
• /apps/frontend/railway.json — same for frontend + docker/frontend.Dockerfile.

The GitHub-connected BACKEND service uses the default config at repo root (railway.json).
Railpack / “No start command” is avoided because builder is explicitly DOCKERFILE.

For a FRONTEND service: Railway → that service → Settings → Config-as-code (or “Config file”)
and set the path to the absolute repo path:

  /apps/frontend/railway.json

If you skip that, the frontend service would still read root railway.json and build the API image.

(Root Dockerfile symlink is optional extra; railway.json is the source of truth for builder.)

--- Variables (repo templates + Railway UI) ---
Docker images do not bake in secrets. Railway injects env at **container runtime**.
We committed templates at repo root so Railway can **suggest** variables:

  .env.railway.backend   → import into the **backend** service
  .env.railway.frontend  → import into the **frontend** service

After connecting GitHub, open each service → **Variables** → use the flow that
offers “import from .env” / suggested vars (Railway scans root `.env.*` files).

Fill **GEMINI_API_KEY** (and any other empty keys) in the UI — never commit real keys.

In those files, **MySQL** = your DB service tile name; **foldwatch** = your backend
service name (rename in the file if yours differs, then re-import or edit in UI).

--- Flow ---
1) Add MySQL (Database → MySQL).
2) Backend: GitHub repo, root directory empty — uses /railway.json automatically.
3) Frontend: new service, same repo, root empty — set config file /apps/frontend/railway.json,
   then add INTERNAL_API_URL + Generate Domain.
4) Generate Domain on backend if you need a public API URL.

--- Same thing from the terminal (Railway CLI) ---
Prereq: `railway login` — https://docs.railway.com/cli

From this repo root, link the project + service (names/IDs from dashboard URL):

  railway link -p <PROJECT_ID> -s <BACKEND_SERVICE_NAME>

Apply backend vars from the template (preserves ${{MySQL…}} syntax):

  ./scripts/railway-import-env.sh <BACKEND_SERVICE_NAME> .env.railway.backend

Set a secret without shell quoting issues:

  printf '%s' 'YOUR_GEMINI_KEY' | railway variable set GEMINI_API_KEY --stdin -s <BACKEND_SERVICE_NAME>

Redeploy that service:

  railway redeploy -s <BACKEND_SERVICE_NAME>

Public Railway domain (max one free domain per service):

  railway domain -s <BACKEND_SERVICE_NAME>
  railway domain -s <FRONTEND_SERVICE_NAME>

Frontend env (after you create a second GitHub service for the UI):

  railway link -p <PROJECT_ID> -s <FRONTEND_SERVICE_NAME>
  ./scripts/railway-import-env.sh <FRONTEND_SERVICE_NAME> .env.railway.frontend

CI / headless: create a Project Token in Railway, then:

  RAILWAY_TOKEN=... railway variable set ...

Limitation: the “Config file path” = /apps/frontend/railway.json is not available in the
public CLI last we checked — set that once in the dashboard for the frontend service, or
the frontend service will keep using root railway.json (backend image).

Notes
-----
- Backend listens on process.env.PORT (Railway sets this automatically).
- Playwright image is heavy; pick a plan with enough RAM/CPU.
- Never commit real API keys; set secrets only in Railway Variables.
